{
  "name": "betterft8-client",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "mock": "node mock-server.mjs",
    "mock:build": "vite build && node mock-server.mjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5",
    "svelte": "^5",
    "svelte-check": "^4.7.1",
    "typescript": "^5",
    "vite": "^6",
    "ws": "^8"
  }
}
